spring:
  application:
    #对应config server获取的配置文件的{application}
    name: microservice-foo
  cloud:
    config:
      uri: http://localhost:8090/
      #对应配置文件的profile
#      profile:
      #对应配置文件的{label}
      label: master